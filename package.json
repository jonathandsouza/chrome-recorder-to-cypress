{
	"name": "chrome-recorder-to-cypress",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"directories": {
		"test": "tests"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "@cypress/chrome-recorder ./tests/*.json --output=integrations"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@cypress/chrome-recorder": "^2.2.0"
	}
}
